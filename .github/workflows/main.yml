name: simple

on: [push]

jobs:
  build:
    runs-on:
      - self-hosted
      - Linux

    steps:
      - name: test node js
      - uses: actions/checkout@2
      - run: |
          echo Hello, world!
          node -v