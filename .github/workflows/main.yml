name: simple

on: [push]

jobs:
  print-username:
    runs-on: self-hosted

    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: Check node v2
        run: | 
          node -v
          echo hello, world
