name: simple

on: [push]

jobs:
  build:
    runs-on:
      - self-hosted
      - Linux

    steps:
      - uses: actions/checkout@v2

      - name: test node js
      - run: |
          echo Hello, world!
          node -v