name: simple

on: [push]

jobs:
  print-username:
    runs-on: self-hosted

    steps:
      - uses: action/checkout@v2
      - name: Check node v2
        run: | 
          node -v
          echo hello, world
